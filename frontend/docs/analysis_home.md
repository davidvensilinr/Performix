we are building the basic analysis home page where a user can add their organisation or could select an organisation to view the analysis of that respective organisation that they have added

import navbar from components as usual 

Let's have components
1. Add Organisation button
2. Select Organisation Text 

But we should align these in the center for which we are gonna use flex container with flex-col items-center justify-center gap-4 so that the componets are aligned in the center vertically with gap of 4 between them